# v1.3.7 COLORSYNC

## 已修复

- 完全去掉按钮控制的 Matrix Demo 功能：B3+B6 被消费但不会进入 Demo。
- 按钮模式和 Web 模式颜色同步：按钮切换默认 faces 时使用首页设定颜色。
- 按钮切换默认 faces 后，`requestState.face370` 会反映当前 370 LED 显示。
- WebUI 表情部件页新增随机组合按钮。
- WebUI 表情部件页新增左右眼同步开关。
- WebUI 颜色上传/下载和自动同步会更新页面 accent 色与预览。

## 保留

- B2+B6 继续滚动显示 `IP  SSID`。
- Bad Apple 仍排除。
- Unity Voice/Music/Video TimeLineDb 保留。
