# 1.4.9 Web 稳定与整理

- 移除 1.4.7 / 1.4.8 末尾重复运行补丁，改为单一稳定模块。
- 移除会反复触发的全页面 MutationObserver，避免页面卡死/崩溃。
- 保存列表统一由一个模块管理：默认表情锁定、支持重命名、两个页面同步。
- 表情部件完整预览固定在页面顶部，并与自定义 370 LED 编辑器隔离。
- 滚动文字开始/停止使用单一状态机，停止时清理旧版与新版计时器。
- Unity 媒体时间轴按 18x16 -> 22x18 居中映射，保留 mm:ss 时间条。
- 亮度只保留数字输入框和滑块同步，不再显示额外数值。

